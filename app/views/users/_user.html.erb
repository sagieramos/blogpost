<li>
  <% if user.photo.present? %>
    <img class="user-photo" src="<%= user.photo %>" alt="<%= "#{user.name}'s photo" %>">
  <% else %>
    <div class="placeholder-box"></div><br>
  <% end %>
  <div>
    <h3 class="user-name"><%= user.name %></h3>
    <p>Number of Posts: <%= user.posts_counter %></p>
  </div>
</li>
